# 이 파일은 **분석되면 안 된다.**
#
# `.ipriskignore` 의 `vendor/*` 에 걸려 변경 이벤트조차 생기지 않아야 한다.
# 만약 이 파일에서 Risk 가 생기면 무시 규칙이 GitHub 마운트에서 동작하지 않는
# 것이므로, 그 자체가 결함이다.
#
# 다른 샘플과 겹치지 않는 패키지를 골라, 새어 나오면 바로 눈에 띄게 했다.
flask==3.0.3
