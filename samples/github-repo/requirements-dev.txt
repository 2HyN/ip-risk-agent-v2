# 접두 일치로 읽히는 것을 확인하는 파일이다.
#
# 예전에는 커넥터가 `requirements.txt` 라는 정확한 이름만 의존성으로 인정해,
# 이 파일은 읽을 수 있는데도 Patent 분석으로 갔다.

# NOTICE_REQUIRED — 고지와 사본 첨부로 충족된다 (MIT).
pytest==8.3.2

# VERSION_RANGE_NOT_PINNED — 확정 버전이 아니라 범위다.
# 조회는 성공하므로 coverage 는 COMPLETE 를 유지하고 진단만 붙는다.
ruff>=0.6.0
