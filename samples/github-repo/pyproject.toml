# PEP 621 매니페스트. `project.dependencies` 를 읽는다.
[project]
name = "iprisk-github-sample"
version = "0.1.0"
requires-python = ">=3.12"

# REVIEW_REQUIRED — 결합 방식에 따라 의무가 달라져 사람이 판단해야 한다 (MPL-2.0).
dependencies = [
    "certifi==2024.7.4",
]

[project.optional-dependencies]
docs = ["mkdocs==1.6.1"]
